# MCP MVP Verification — 2026-08-23

## Scope

공식 MCP Python SDK 2.0 client가 bearer 인증된 Streamable HTTP `/mcp` endpoint를 통해
HTTP와 같은 `GatewayService`와 PostgreSQL query 경계를 사용하는지 검증했다.

## Commands And Result

```text
uv run ruff check .                 PASS
uv run mypy src                     PASS (17 source files)
uv run pytest                       PASS (89 unit tests)
uv run pytest -m integration        PASS (7 integration/load tests)
uv run query-man-verify             PASS (9/9 golden questions)
quick_validate.py query-man skill   PASS
```

## Evidence

| Contract | Executed evidence | Result |
|---|---|---|
| Transport | Official client connects to stateless JSON Streamable HTTP `/mcp` | PASS |
| Authentication | The same bearer token succeeds on HTTP `/sources` and MCP; missing token is rejected | PASS |
| Fixed tools | `list_sources`, `get_context`, `query` names and bounded input JSON schemas | PASS; no host or credential input |
| Authorization | Restricted caller sees only its source and denied source matches unknown-source error | PASS |
| Answerability | `unsupported` and `needs_clarification` responses plus explicit Skill stop conditions | PASS |
| Revision retry | Stale revision is rejected, context is refreshed, and the query succeeds with the refreshed revision | PASS |
| Golden workflow | 4 development + 5 market questions execute from context selection through result hashing | 9/9 match |
| Payload budget | MCP metadata bytes, result bytes and rows stay inside each source budget | PASS |
| Common Skill | Frontmatter, discovery metadata and scaffold completion validation | PASS |

## Remaining Dependency

MCP MVP는 현재 process-local metadata cache가 제공하는 active revision을 소비한다.
Control plane의 immutable snapshot publish와 rollback은 `META-05`, `META-06`에서 별도로
완료해야 하며 tool schema나 gateway safety 경계를 바꾸지 않는다.
