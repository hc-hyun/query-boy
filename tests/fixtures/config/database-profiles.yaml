version: 1

profiles:
  fixture:
    host: 127.0.0.1
    host_env: QUERY_MAN_POSTGRES_HOST
    port: 5432
    port_env: POSTGRES_PORT
    database: query_man
    sslmode: disable
    authentication:
      type: password
      password_env: FIXTURE_SOURCE_READER_PASSWORD
