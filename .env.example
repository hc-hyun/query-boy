POSTGRES_DB=query_man
POSTGRES_USER=query_man_admin
POSTGRES_PASSWORD=replace-with-a-local-secret
POSTGRES_PORT=5432
TZ=Asia/Seoul
DEVELOPMENT_ISSUES_READER_PASSWORD=replace-with-a-local-secret
MARKET_VOC_READER_PASSWORD=replace-with-a-local-secret
SUPPORT_TICKETS_READER_PASSWORD=replace-with-a-local-secret
QUERY_MAN_HOST=127.0.0.1
QUERY_MAN_PORT=3000
QUERY_MAN_LOG_LEVEL=info
# Required when QUERY_MAN_HOST is not a loopback address.
# QUERY_MAN_API_TOKEN=replace-with-at-least-32-random-characters
# For multiple callers, use QUERY_MAN_ACCESS_POLICY_FILE instead of QUERY_MAN_API_TOKEN.
# QUERY_MAN_ACCESS_POLICY_FILE=config/access-policies.yaml
QUERY_MAN_METADATA_CACHE_TTL_MS=30000
QUERY_MAN_METADATA_MAX_STALE_MS=300000
QUERY_MAN_METADATA_RETRY_DELAY_MS=5000
# Optional PostgreSQL control plane. Use a dedicated login with SELECT/INSERT on
# control.metadata_snapshots and SELECT/INSERT/UPDATE on control.active_metadata_revisions.
# QUERY_MAN_CONTROL_DSN=host=127.0.0.1 port=5432 dbname=query_man user=query_man_control password=replace-me sslmode=verify-full
# Enables encrypted source profile revisions and no-restart admin onboarding.
# Generate with: openssl rand -base64 32 | tr '+/' '-_'
# QUERY_MAN_SOURCE_ENCRYPTION_KEY=replace-with-urlsafe-base64-32-byte-key
QUERY_MAN_SOURCE_RELOAD_INTERVAL_MS=5000
